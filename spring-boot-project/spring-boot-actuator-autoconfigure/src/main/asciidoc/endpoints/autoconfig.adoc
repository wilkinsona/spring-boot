[[autoconfig]]
= Auto-configuraton Report (`autoconfig`)

The `autoconfig` endpoint provides information about the application's auto-configuration.



[[autoconfig-retrieving]]
== Retrieving the Report

To retrieve the auto-configuration report, make a `GET` request to
`/application/autoconfig`, as shown in the following curl-based example:

include::{snippets}autoconfig/curl-request.adoc[]

The resulting response is similar to the following:

include::{snippets}autoconfig/http-response.adoc[]



[[autoconfig-retrieving-response-structure]]
=== Response Structure

The response contains details of the application's auto-configuration. The following table
describes the structure of the response:

[cols="3,1,3"]
include::{snippets}autoconfig/response-fields.adoc[]
